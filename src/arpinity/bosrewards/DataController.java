package arpinity.bosrewards;

public class DataController {
	//TODO: Create code that manages plugin data (users and rewards catalogue)
	// Flatfiles should be handled with config api
	
	//TODO: Create database API for the rest of the plugin to interface with
	//Determine what kind of database is being used
	//Determine what config options (if applicable) should be used
	//Construct a database object that has generic external methods that use
	//		the methods and connectivity specific to that database type.
	//In theory, all this allows us to just add database types as we need to support them.
	
	
	//"Get data"
	// user
	// reward
	
	
	//"Set data"
	// user
	// reward
	
	
	
}
